# {{ function.name }}
{% if function.namespace is not empty %}**namespace:** {{ function.namespace }}{% endif %}


{{ function.summary|raw }}

```php
function {{ function.name }}( {% for argument in function.arguments %}
{{- argument.types ? argument.types|join('|')~' ' }}
{{- argument.byReference ? '&' }}
{{- argument.name }}{{ argument.default ? ' = '~argument.default }}
{%- if not loop.last %}, {% endif %}
{%- endfor %} )
{{- function.response.types ? ': '~function.response.types|join('|') }}
```

{% if function.description is not empty %}
**description:** {{ function.description|raw }}
{% endif %}

{% if function.deprecated %}
#### Warning:
This function is **deprecated**. This means that this function will likely be removed in a future version.
{% endif %}

{% include 'arguments.md.twig' with {'arguments': function.arguments } %}

{% include 'responses.md.twig' with {'response': function.response } %}

{% if function.tags.see is not empty %}
#### **See Also:**
{% for see in function.tags.see %}
  * {{ see.reference }} {% if see.description %}- {{ see.description|raw }}{% endif %}
{% endfor %}
{% endif %}

{% if function.tags.link is not empty %}
{% for link in function.tags.link %}
#### {{ link.link }} {% if link.description and link.description != link.link %}- {{ link.description }}{% endif %}
{% endfor %}
{% endif %}{# function.tags.see || function.tags.link #}

